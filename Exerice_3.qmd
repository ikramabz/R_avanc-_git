---
title: "Seance_3"
format: pdf
editor: visual
---

# Question 1

*1. Créez un dossier data/, et copiez le jeu de données à l’intérieur du dossier.* *Initialisez un script .R ou Quarto à la racine du projet.* *Créez un premier commit comprenant l’intégralité des modifications, en excluant le fichier csv.* *Initiez un nouveau projet sur GitHub vide (sans README) que vous appellerez cours_r_partie_3. Dans un terminal, dans le projet RStudio, configurez le serveur distant en utilisant l’expression suivante : git remote add origin \<l'adresse de votre répertoire\> Faites un push. Commentez. Dans la suite, nous ferons un commit intitulé question X après larésolution de chaque question, suivi d’un push sur le serveur distant.*

a)  Pour exclure le fichier csv on ouvre le fichier .gitignore et ajoute : basevelo/\*.csv (si y'a pas gitignore alors on ouvre un nouveau texte (textfil) qu'on nomme .gitignore)
b) envoyer dans terminal : git push -u origin main


# Question 2


```{r}
df_velo <- read.csv("basevelo.csv", sep = ";")
```

```{r}
#afficher tous les numeros de boucles pour en choisir deux
unique(df_velo$Numéro.de.boucle)
```
```{r}
#on creer les dataframes avec les deux numeros qu'on a choisis 
df_b_746 <- df_velo[df_velo$Numéro.de.boucle == 746, ]
df_b_787 <- df_velo[df_velo$Numéro.de.boucle == 787, ]
```

```{r}
#on affiche les dates pour choisir les deux qu'on veut 
unique(df_velo$Jour)
```

```{r}
#on ajoute les dates qu'on veut dans deux dataframe, ici j'ai choisi 21 sept et 28 mai
df_d_0921 <- df_velo[df_velo$Jour == "2025-09-21", ]
df_d_0528 <- df_velo[df_velo$Jour == "2025-05-28", ]
```



# Question 3

Implémentez une fonction filtre_anomalie(). Cette fonction prendra en entrée un 
data.frame respectant le même schéma que notre jeu de données df_velo. Elle retournera le 
même data.frame sans les lignes contenant des anomalies Forte ou Faible. Appliquez cette 
fonction sur les data.frame définis ci dessus. Combien de lignes sont filtrées ?

```{r}
# fonction qui supprime les lignes avec anomalies "Forte" ou "Faible"
filtre_anomalie <- function(df) {
  
  # on garde uniquement les lignes qui NE sont PAS "Forte" ou "Faible"
  df_clean <- df[
    df$Probabilité.de.présence.d.anomalies != "Forte" &
    df$Probabilité.de.présence.d.anomalies != "Faible", 
  ]
  
  # on retourne le dataframe nettoyé
  return(df_clean)
}
```


```{r}
# application sur les boucles
df_b_746_clean <- filtre_anomalie(df_b_746)
df_b_787_clean <- filtre_anomalie(df_b_787)

# application sur les jours
df_d_0921_clean <- filtre_anomalie(df_d_0921)
df_d_0528_clean <- filtre_anomalie(df_d_0528)
```

```{r}
# nombre de lignes supprimées pour chaque dataframe

# boucle 746
nb_suppr_746 <- nrow(df_b_746) - nrow(df_b_746_clean)

# boucle 787
nb_suppr_787 <- nrow(df_b_787) - nrow(df_b_787_clean)

# jour 21 septembre
nb_suppr_0921 <- nrow(df_d_0921) - nrow(df_d_0921_clean)

# jour 28 mai
nb_suppr_0528 <- nrow(df_d_0528) - nrow(df_d_0528_clean)


# afficher les résultats dans la console
nb_suppr_746
nb_suppr_787
nb_suppr_0921
nb_suppr_0528
```
La fonction filtre_anomalie permet de supprimer les lignes contenant des anomalies de type "Forte" ou "Faible". Après application : 
21 lignes ont été supprimées pour la boucle 746 
4 lignes ont été supprimées pour la boucle 787 
6 lignes ont été supprimées pour le jour 21 septembre 
11 lignes ont été supprimées pour le jour 28 mai 


# Question 4

Implémentez une fonction compter_nombre_trajets(). Cette fonction prendra en entrée un 
data.frame respectant le même schéma que notre jeu de données df_velo. Elle comptera le nombre total de trajets référencés. Appliquez cette fonction sur les data.frame définis ci dessus.

```{r}
# fonction qui calcule le nombre total de trajets
compter_nombre_trajets <- function(df) {
  
  # on additionne toutes les valeurs de la colonne Total
  total_trajets <- sum(df$Total, na.rm = TRUE)
  
  # on retourne le résultat
  return(total_trajets)
}
```

```{r}
# calcul pour les boucles
trajets_b_746 <- compter_nombre_trajets(df_b_746)
trajets_b_787 <- compter_nombre_trajets(df_b_787)

# calcul pour les jours
trajets_d_0921 <- compter_nombre_trajets(df_d_0921)
trajets_d_0528 <- compter_nombre_trajets(df_d_0528)
```


```{r}
trajets_b_746
trajets_b_787
trajets_d_0921
trajets_d_0528
```


# Question 5

```{r}
# fonction qui compte le nombre de boucles différentes
compter_nombre_boucle <- function(df) {
  
  # on récupère les numéros de boucle uniques
  nb_boucles <- length(unique(df$Numéro.de.boucle))
  
  # on retourne le résultat
  return(nb_boucles)
}
```


```{r}
# sur les boucles
compter_nombre_boucle(df_b_746)
compter_nombre_boucle(df_b_787)

# sur les jours
compter_nombre_boucle(df_d_0921)
compter_nombre_boucle(df_d_0528)
```
Les résultats montrent que les data.frames correspondant aux boucles 746 et 787 contiennent chacun une seule boucle, ce qui était attendu puisqu’ils ont été construits à partir d’un numéro de boucle unique.
En revanche, les data.frames correspondant aux jours (21 septembre et 28 mai) contiennent chacun 56 boucles différentes, ce qui correspond à l’ensemble des boucles présentes dans le jeu de données pour un jour donné.
Ces résultats étaient donc prévisibles : un filtrage par boucle donne une seule boucle, tandis qu’un filtrage par jour conserve toutes les boucles.




# Question 6 

Implémentez une fonction trouver_trajet_max(). Cette fonction identifiera quel est la paire 
bouclejour avec le maximum de passages décomptés. Elle retournera un objet contenant :
• Le nom de la boucle
• Le jour correspondant au maximum de passage enregistrés
• Le nombre de passages enregistrés

```{r}
# fonction qui trouve le maximum de trajets
trouver_trajet_max <- function(df) {
  
  # on trouve la ligne avec le maximum de Total
  ligne_max <- df[which.max(df$Total), ]
  
  # on crée un petit résultat propre
  resultat <- list(
    boucle = ligne_max$Boucle.de.comptage,
    jour = ligne_max$Jour,
    nb_trajets = ligne_max$Total
  )
  
  # on retourne le résultat
  return(resultat)
}
```


```{r}
trouver_trajet_max(df_b_746)
trouver_trajet_max(df_b_787)

trouver_trajet_max(df_d_0921)
trouver_trajet_max(df_d_0528)
```
La fonction trouver_trajet_max permet d’identifier, pour chaque data.frame, la paire boucle-jour présentant le nombre maximal de trajets.

Pour la boucle 746, le maximum de trajets est atteint le 29 avril 2025 avec 1334 trajets.
Pour la boucle 787, le maximum est atteint le 1er octobre 2025 avec 2964 trajets.

Concernant les data.frames par jour, le maximum de trajets pour le 21 septembre 2025 est observé sur la boucle 890 – Pont de Pirmil vers Sud avec 1684 trajets.
Pour le 28 mai 2025, le maximum est atteint sur la boucle 785 – 50 Otages vers Sud avec 3328 trajets.

Ces résultats montrent que certaines boucles concentrent davantage de passages selon les jours, ce qui peut refléter des variations de fréquentation selon les périodes ou les localisations.


# Question 7 

Appliquer la fonction trouver_trajet_max() au jeu de donnée complet df_velo. Quelle est 
votre analyse ? Proposez une modification de la fonction filtre_anomalie().


```{r}
# appliquer sur toute la base
trouver_trajet_max(df_velo)
```

Le résultat obtenu est très élevé (345780 trajets), ce qui n’est pas réaliste. Cela s’explique par la présence d’anomalies dans les données. Il est donc nécessaire de filtrer les données avant d’appliquer la fonction.


```{r}
#option 1 : 

filtre_anomalie <- function(df) {
  
  df_clean <- df[
    is.na(df$Probabilité.de.présence.d.anomalies) |
    df$Probabilité.de.présence.d.anomalies == "",
  ]
  
  return(df_clean)
}

df_clean <- filtre_anomalie(df_velo)
trouver_trajet_max(df_clean)
```


```{r}
# option 2 remodifier toute la fonction 

# version améliorée qui filtre automatiquement
trouver_trajet_max <- function(df) {
  
  # on filtre les anomalies
  df <- filtre_anomalie(df)
  
  # on cherche le max
  ligne_max <- df[which.max(df$Total), ]
  
  return(list(
    boucle = ligne_max$Boucle.de.comptage,
    jour = ligne_max$Jour,
    nb_trajets = ligne_max$Total
  ))
}
```

